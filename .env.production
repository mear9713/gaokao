# ─── 生产环境后端 API ───────────────────────────────────
# ⚠️ 重要：后端当前为 http://124.221.141.149:18001（HTTP + 纯 IP，无证书）。
# 前端部署在 HTTPS（Vercel）时，浏览器会拦截对 HTTP 接口的请求（Mixed Content）。
# 上线前必须满足下列任一条件：
#   方案 A：后端启用 HTTPS（配域名+证书），此处填  https://你的后端域名/api
#   方案 B：用 vercel.json rewrites 把 /api 代理到后端，此处保持 /api
# 当前默认 /api，需配合 vercel.json 代理才能在生产生效。
VITE_API_BASE_URL=/api
